So two ways to yse gRPC

1. Statically, using protocole buffer (.proto file) to re-process code in JS.

    Create proto file and use protoc arguments at command line

2. Dynamically,  proto files loaded at runtime , using Protobuf.js.